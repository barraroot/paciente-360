/**
 * Tailwind v4 stub — a configuração canônica vive em
 * `resources/css/app.css` via diretiva `@theme`. Mantemos este arquivo
 * apenas para detecção por editores/IDEs (IntelliSense, prettier-plugin-tailwindcss)
 * e como ponto de extensão futuro caso seja necessário um plugin JS
 * (forms, typography). Quando isso acontecer, importar via
 * `@plugin '@tailwindcss/forms'` no CSS é a forma preferida.
 *
 * Não duplique tokens aqui — eles têm fonte única no `@theme` do CSS.
 */
export default {};
